{
	"id": "/grape/forgot_password",
	"description": "Resets and send a new password to a user. Provide either email, or a user_id and username",
	"sqlfunc": "grape.forgot_password",
	"sqlfunctype": "jsonb",
	"name": "GrapeForgotPassword",
	"roles": ["guest"],
	"type": "object",
	"properties": {
		"email": {"type": "string", "description": "E-mail for the user to reset"},
		"username": {"type": "string", "description": "Username for the user to reset (use in conjunction with user_id)"},
		"user_id": {"type": "number", "description": "user_id for the user to reset (use in conjunction with username)"}
	},
	"return": {
		"type": "object",
		"properties": {
			"status": {"enum": ["ERROR", "OK"]},
			"message": {"type": "string"},
			"code": {"type": "number"},
			"error": {"type": "object"}
		}
	}
}
